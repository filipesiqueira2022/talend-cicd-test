name: Talend CI Test

on:
  push:
    branches:
      - main

jobs:

  executar-job:

    runs-on: ubuntu-latest

    steps:

      - name: Checkout código
        uses: actions/checkout@v4

      - name: Listar arquivos
        run: ls -R
